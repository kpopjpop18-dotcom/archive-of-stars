{
  "name": "atiny-archive",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first K-pop photocard gacha PWA",
  "scripts": { "dev": "netlify dev", "deploy": "netlify deploy --prod" },
  "dependencies": { "sharp": "^0.33.5" }
}
